# Vercel KV (optional - for persistent storage in production)
# Get these from Vercel dashboard after linking KV database
KV_REST_API_URL=
KV_REST_API_TOKEN=

# For local development, data will be cached in memory
# In production on Vercel, it uses KV storage if available
